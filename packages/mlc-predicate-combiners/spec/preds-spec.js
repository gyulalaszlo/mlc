"use strict";

import {nothing} from '../src/preds'

require('mlc-minispec').nodeJs(
    (describe, it, expect) => {

        describe('nothing', () => {
            it('should match nothing', () => {

            });
        });

    });
